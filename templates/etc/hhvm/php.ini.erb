;php options
<% if scope.lookupvar("hhvm::date_timezone") != '' %>
date.timezone = <%= scope.lookupvar('hhvm::date_timezone') %>
<% end %>

<% if scope.lookupvar('hhvm::php_ini_cfg_append') and !scope.lookupvar('hhvm::php_ini_cfg_append').empty? -%>
;additional options
	<% scope.lookupvar('hhvm::php_ini_cfg_append').each do |key, val| -%>
<%= key %>=<%= val %>
	<% end -%>
<% end -%>
  